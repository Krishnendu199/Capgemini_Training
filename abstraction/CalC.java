package abstraction;

interface CalC 
{
	void add(int a, int b);//"public abstract" will be add by default,explicit mention for others.
	void mul(int a,int b);
	void sub(int a, int b);
	
	//protected and private allowed.
	
}
